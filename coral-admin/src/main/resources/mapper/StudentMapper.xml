<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gemframework.modules.extend.student.mapper.StudentMapper">

    <insert id="insert1" parameterType="String">
        insert into tb_student(name) values(#{name})
    </insert>

    <insert id="insert2" parameterType="String">
        insert into tb_student(name) values(#{name})
    </insert>

    <insert id="insert3" parameterType="String">
        insert into tb_student(name) values(#{name})
    </insert>
</mapper>




